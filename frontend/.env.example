# Point the frontend at your backend
VITE_SHIPPING_API_URL=http://localhost:4000

# Optional: the checkout popup already gets the key id from the backend's
# create-order response, so you usually DON'T need this. Included for reference.
# NEVER put RAZORPAY_KEY_SECRET in any VITE_ variable — it ships to the browser.
VITE_RAZORPAY_KEY_ID=rzp_test_TDkfX2Oox2CgwF
